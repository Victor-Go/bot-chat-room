import { User } from "../types/user";

export const ON_USER_JOINED = "chat/ON_USER_JOINED";

const initialState = {

}

export default (state = initialState, action = {}) => {
}

export const onUserJoined = (user: User) => {

}